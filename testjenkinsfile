pipeline{
    agent any

    stages{
        stage('Dependency install'){
            agent {docker {image 'nodejs'
            args '-u root:sudo'}}
            steps{
                sh 'npm cache clean'
                sh ' sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
            }
        }
    }
}
